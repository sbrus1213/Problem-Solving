// BOJ 14285 간선 끊어가기
// 

#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
using namespace std;
const int mxn = 501;

int main() {
	cin.tie(NULL); cout.tie(NULL);
	ios_base::sync_with_stdio(false);

	int n, m; cin >> n >> m;

	v
	int tot = 0;
	for (int i = 0; i < n; ++i) {
		int u, v, w; cin >> u >> v >> w;
		E.push_back({ u, v, w });
		tot += w;
	}


}